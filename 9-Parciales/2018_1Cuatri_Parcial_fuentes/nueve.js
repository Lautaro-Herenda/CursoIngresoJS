function mostrar()
{
var marca;
var peso;
var temperatura;
var seguir;
var contadorPar;
var flag;
var presio;
var maximo;
var pesoMax;
do{
    marca = prompt("Ingrese una marca: ");
    peso = parseInt(prompt("ingrese un peso entre 1 y 100"))
    while (peso < 1 || peso > 100 || isNaN(peso)) {
        marca = prompt("Eso no es una marca. Ingrese una marca: ");
    }
    temperatura = parseInt(prompt("Ingrese un temperatura entre -30 y 30: "));
    while (temperatura < -30 || temperatura > 30 || isNaN(temperatura)) {
        temperatura = parseInt(prompt("temperatura invalida. Ingrese un temperatura entre -30 y 30: "));
}
    if (temperatura % 2 == 0) {
        contadorTempPares++;

    if  ((flag == 0 || peso ))             







    }


}
